<h2>All Ingredients</h2>

<ul>
  <% @ingredients.each do |ingredient| %>
  <li><%= ingredient.name %>
  <%= link_to "Edit Ingredient", edit_ingredient_path(ingredient), class: "btn btn-default btn-sm" %>
  </li>
  <% end %>
</ul>
